#
# Enough to run headless and leave a readable log. Everything else is
# left at weechat's own defaults on purpose.
#

[look]
# No prompt to confirm /quit — this runs unattended.
confirm_quit = off

[network]
gnutls_ca_system = on
